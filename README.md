# fashion-mnist-cnn
A convolutional neural network (CNN) built from scratch in PyTorch to classify fashion products using the Fashion-MNIST dataset, with proper training, evaluation, and feature visualization.
